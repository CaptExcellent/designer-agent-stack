This block is generated from the shared stack; edit its repository source.
Project AGENTS.md instructions take precedence over these personal defaults.
Skills are user-level Agent Skills. Serena uses the codex MCP context.
In the desktop app, activate the real workspace explicitly when its cwd differs.
Check the connection with /mcp. Serena hooks are opt-in; if enabled, review /hooks.
