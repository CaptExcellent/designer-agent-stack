This block is generated from the shared stack; edit its repository source.
Project CLAUDE.md instructions take precedence over these personal defaults.
Skills are user-level Claude Code skills. Serena uses the claude-code MCP context.
Check /mcp for connectivity. Optional reminder hooks guide retrieval; they do not grant
automatic tool permissions or replace the client's system prompt.
Launch Claude from the intended repository: Serena's Claude context binds to
that single project at startup and may omit activate_project. Restart from the
correct directory to switch projects. Tiny obvious edits can use ordinary files.
